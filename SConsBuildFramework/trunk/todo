TOTEST:
-------

[DONE] o DEPENDENCIES and MULTI-DEPENDENCIES.


TOFIX:
------

[DONE] o pdb not generated (see env['PDB'])


TODO:
-----

[DONE] o OSError: [Errno 2] No such file or directory: 'C:\\cygwin\\tmp\\nf-vgsdk\\SrcLib\\Virtuals' => if a project (from deps option) is not founded, skip it.
[TODO] o create doxygen.
[DONE] o create automatically a package with files and dependencies.
[DONE] o cache = on/off
o SetOption('implicit_cache', 1), SourceSignatures('MD5') SourceSignatures('timestamp')
o idea for a new project (with python-soappy) => myOpenBuildFarm
o new type of project : headers.
o vgsdkSamples support
o vgsdk configure deps in all projects.
o posix platform (could be)=> linux or bsd.
o "Doom3's Linux build system uses SCons. CC="ccache distcc g++-3.3" JOBS=8 rocks!"
[PARTIAL] o a more complete checkSBF => check SCONS_BUILD_FRAMEWORK env var (terminated by a /)
o print 'num_jobs', env.GetOption('num_jobs')
o targets mrproper, clean => clean, uninstall, mrproper
o add scons setConfig='debug' (switch from debug to release with scons cmd)
o update Makefile (used as a bridge between some IDE and scons).
o Ajout du fichier licence GPL et des entetes dans tous les fichiers
[PARTIAL] o svn (checkout and update). TBT update for a non svn project.
o stat. (number of lines... see sloccount, http://sourceforge.net/projects/cccc)
o search path for projects (used by deps).
o compile more than one config (=['debug', 'release']).
o custom config ? add (subconfigname.options).
o check compatibility with scons@work
o put all .sconsign files in one place (env.SConsignFile()) ?
o /usr/bin/g++ -UNDEBUG on gcc for debug => -DDEBUG ?
o utilisation du # pour les paths relatifs/absolus ?
o zero conf or script Python for automatic/interactive configuration.
